#!/bin/sh

set -o errexit
set -o nounset

readonly PALETTE=Mocha

if [ ! -s "Catppuccin ${PALETTE}.clr" ]; then
  curl -fLsS -o "Catppuccin ${PALETTE}.clr" \
    "https://raw.githubusercontent.com/catppuccin/palette/main/clr/Catppuccin%20${PALETTE}.clr"
fi

exit 0
