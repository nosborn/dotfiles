# Shell Scripts

When writing or modifying shell scripts, you MUST:

1. Validate all shell scripts against shellcheck. Apply rules from `~/.config/shellcheckrc` in addition to default rules.
2. Format all shell scripts using `shfmt -i 2 -ci` (2-space indentation, case statement body indentation).
