---
apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  # selector:
  #   app.kubernetes.io/name: MyApp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9376
---
apiVersion: v1
kind: Service
metadata:
  name: my-service
spe:
  # selector:
  #   app.kubernetes.io/name: MyApp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9376
---
apiVersion: apps/v1beta2
kind: DaemonSet
metadata:
  name: deprecated-daemonset
