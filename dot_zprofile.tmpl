{{ if eq .where "work" -}}
# The following lines were added by Docker Desktop to add commands to your PATH.
export PATH="${PATH}:{{ .chezmoi.homeDir }}/.docker/bin"
# End of Docker Desktop section.

eval "$({{ .homebrew.prefix }}/bin/festool helpers generate)"
{{- end }}
